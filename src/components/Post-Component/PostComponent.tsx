import type {IPost} from "../../models/IPost.ts";

const PostComponent = () => {

    type PostPropType = {
        post: IPost
    }

    return (
        <div>

        </div>
    );
};

export default PostComponent;